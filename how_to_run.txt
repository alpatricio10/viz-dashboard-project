1. Create and activate a virtual environment (Optionally)
3. Install dependencies:
   pip install -r requirements.txt
4. Run the dashboard:
   streamlit run viz.py
5. Open the local URL shown in the terminal (usually http://localhost:8501).
